export interface NoteSummary {
    id: number;
    name: string;
};

export interface Note {
    id: number;
    name: string;
    content: string; 
};

export interface NoteContent {
    id: Number;
    content: string;
};